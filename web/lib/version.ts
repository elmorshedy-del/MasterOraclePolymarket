/**
 * Single source of truth for the dashboard's user-visible version + phase.
 * Imported by app/layout.tsx so the header badge and footer can never
 * disagree across surfaces.
 */

export const VERSION = "0.5.0";
export const PHASE_LABEL = "Audit hardened";
export const HAIRCUT_DEFAULT_PCT = 22;
