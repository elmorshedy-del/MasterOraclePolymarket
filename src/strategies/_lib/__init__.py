"""Shared helpers for strategy implementations.

The leading underscore keeps the plugin loader from picking this up. Modules
here are imported by individual strategy.py files, not auto-discovered.
"""
