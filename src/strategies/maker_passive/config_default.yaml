name: default
description: |
  Quote both sides 1 tick inside best on top-volume markets. 60s cooldown
  per (asset, side).

params:
  min_24h_volume_usd: 5000
  target_notional_usd: "50"
  tick_size: "0.01"
  min_spread_ticks: 2
  place_interval_secs: 60
  max_concurrent_positions: 50
