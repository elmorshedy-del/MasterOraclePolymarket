sleeve_id: weather_tail_buy__default
strategy: weather_tail_buy
config_id: default
edge_class: tail
enabled: true
mode: live_full
starting_capital_usd: 5000

params:
  min_price: "0.01"
  max_price: "0.05"
  target_notional_usd: "25"
  price_buffer_bps: 100
  recent_adverse_threshold: "0.005"
  recent_window_secs: 600
  max_concurrent_positions: 100
  category_keywords:
    - weather

market_filter:
  categories:
    - weather

max_concurrent_positions: 100
max_exposure_per_market_usd: 25
loss_management: none
