sleeve_id: momentum_orderbook__default
strategy: momentum_orderbook
config_id: default
edge_class: latency_sensitive
enabled: true
mode: replay_only
starting_capital_usd: 5000

params:
  bullish_threshold: "0.75"
  bearish_threshold: "0.25"
  min_price: "0.20"
  max_price: "0.80"
  min_tob_depth_usd: "200"
  target_notional_usd: "40"
  price_buffer_bps: 100
  cooldown_secs: 120
  max_concurrent_positions: 60

market_filter:
  categories: []

max_concurrent_positions: 60
max_exposure_per_market_usd: 80
loss_management: none
